subprojects {
  apply plugin: 'java'

  repositories {
    mavenLocal()
    mavenCentral()
  }
}
// plugin location
ext.pluginsDir = rootProject.rootDir.path + '/authserver/plugins'

//task build(dependsOn: [':app:uberjar'])
